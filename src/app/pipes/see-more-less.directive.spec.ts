import { SeeMoreLessDirective } from './see-more-less.directive';

describe('SeeMoreLessDirective', () => {
  it('should create an instance', () => {
    const directive = new SeeMoreLessDirective();
    expect(directive).toBeTruthy();
  });
});
